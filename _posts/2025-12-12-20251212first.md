---
layout: single
title:  "[2025-12-12] 20251212 개발일지"
categories: devlog
toc: true
---

## AWS 교육

### AWS 배포 순서 요약
1. 프론트엔드 배포
    1. S3 버킷 생성 및 설정
    2. CloudFront Distribution 생성
    3. Default root object 설정 (index.html)
    4. config.js 업데이트 및 S3 업로드
    5. CloudFront 무효화
2. 백엔드 인프라 구성
    1. VPC 생성 (Public/Private Subnet, NAT Gateway, VPC Endpoint 포함)
    2. 보안 그룹 생성 (VPC Link용 보안 그룹, NLB용 보안 그룹, EC2용 보안 그룹)
    3. IAM 역할 생성 (EC2 인스턴스 프로파일) -> AmazonSSMManagedInstanceCore,  AmazonBedrockFullAccess, AmazonS3ReadOnlyAccess
    4. EC2 인스턴스 생성 (Private Subnet, SSM 사용)
3. 백엔드 배포 및 서비스 시작
    1. S3에서 백엔드 소스 다운로드
    2. Python 환경 설정 (venv, 의존성 설치)
    3. systemd 서비스 설정
4. 로드 밸런서 및 네트워크 구성
    1. 타겟 그룹 생성 (EC2 인스턴스 등록)
    2. Network Load Balancer (NLB) 생성
    3. NLB 헬스체크 확인
    4. VPC Link 생성 (Available 상태 대기)
5. API Gateway HTTP API 설정
    1. HTTP API 생성
    2. Routes 생성 (예: POST /api/chat)
    3. Integration 생성 (VPC Link를 통한 NLM 연결)
    4. 모든 Routes에 Intergration 연결
    5. CORS 설정
6. CloudFront 통합 설정
    1. API Gateway를 CloudFront 원본으로 추가
    2. Behavior 생성 (/api/* 경로 패턴)
    3. 전체 시스템 Test
